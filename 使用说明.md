# 梦鸼博客 · 使用说明

不用懂代码，照着做就行。

> **🌐 网站已经上线：<https://annansup.com>**
>
> 平时的工作流程就一句话：**在本地改东西 → 执行 `npm run deploy` → 等 1 分钟，线上就生效了。**
>
> 本文档里凡是提到「发布」，指的都是执行 `npm run deploy`。**只改不发，网站上不会有任何变化。**

---

## 0. 先会这三件事

### ⭐ 最省事的办法：双击

在 `D:\网站` 文件夹里有**两个现成的启动器，双击就行，不用敲任何命令**：

| 双击这个文件 | 作用 |
| --- | --- |
| **`启动预览.bat`** | 启动本地预览，然后浏览器打开 <http://localhost:4000> |
| **`发布上线.bat`** | 把改动发布到 <https://annansup.com> |

它们已经自动切到正确目录了，**不会出现「找不到 package.json」那类报错**。

> **如果双击后窗口一闪就没**，说明执行出错了、来不及看报错。改用下面敲命令的方式，窗口会停住让你看清错误。

### 敲命令的办法

打开 `D:\网站` 文件夹，在空白处**按住 Shift + 右键 → 「在此处打开 PowerShell 窗口」**。

> ⚠️ **一定要在 `D:\网站` 里打开。** 从开始菜单、Win+X 菜单或「以管理员身份运行」打开的 PowerShell 会停在 `C:\WINDOWS\system32`，在那儿敲任何 `npm` 命令都会报错（见第 11 节）。

### ① 看效果（本地预览）

```
npm run server
```

浏览器打开 <http://localhost:4000> 就能看到，改文件会自动刷新。

### ② 发布上线（改完必须做）

```
npm run deploy
```

这条命令会自动：找出你改了什么 → 提交 → 推送到 GitHub → **GitHub 自动重新生成网站**。
约 1 分钟后打开 <https://annansup.com> 就是新的了。

想写一句自己的提交说明（方便以后回看改了什么）：

```
npm run deploy -- "加了两位朋友的友链"
```

### ③ 停止预览

在预览那个窗口按 `Ctrl + C`，或者直接关掉窗口。

> **每次改完东西，记得 `Ctrl + C` 停掉预览、再 `npm run deploy`。** 预览窗口开着也能发布，但停掉更稳妥。

---

## 一张表：想改什么 → 打开哪个文件

改完**都要执行 `npm run deploy`** 才会在网站上生效。

| 我想改… | 打开这个文件 | 改哪里 |
| --- | --- | --- |
| **加 / 改友情链接** | `source\_data\links.json` | 见第 2 节 |
| 友链的头像图片 | 放进 `source\img\friends\` | 见第 2 节 |
| **发文章** | `npm run new -- "标题" "分类"` | 见第 1 节 |
| 我的昵称、站名 | `_config.yml` | `title:` 和 `author:` |
| 头像 | 替换 `themes\shiguang\source\img\avatar.svg` | 换个同名文件即可 |
| Logo（左上角图标） | 替换 `themes\shiguang\source\img\logo.png` | 建议正方形、透明底 |
| 网易云 / B站 / Steam / Bangumi 卡片 | `themes\shiguang\_config.yml` | 搜 `social:`，改 `link:` 那一行 |
| 音乐 | 把 mp3 丢进 `source\music\` | 文件名就是歌名 |
| 文章分类（"文章"菜单里的归档） | 在 `source\_posts\` 里新建文件夹 | 文件夹名＝分类名 |
| 首页大标题、公告、四个按钮 | `themes\shiguang\_config.yml` | 搜 `banner:` 和 `notice:` |
| 游戏 / 电影 / 动漫列表 | `source\_data\games.json` 等 | 也可以自动抓取（第 9 节） |
| 不想显示某个游戏 | `source\_data\games-hidden.json` | 填 appid 或游戏全名，再跑 `npm run fetch:steam` |
| 某张卡片想换封面 | 覆盖 `source\img\covers\steam\` 里的 `appid.webp` | 豆瓣 / Bangumi 同理 |
| 写我的短评 | `source\_data\movies.json` 等，找那一行的 `"comment"` | |
| 播放器音量 | `themes\shiguang\_config.yml` | `music.volume: 0.7`（0~1） |
| 播放顺序（顺序 / 随机） | `themes\shiguang\_config.yml` | `music.shuffle: false` 改 `true` 就是随机 |
| 页面切换不中断音乐 | `themes\shiguang\_config.yml` | `pjax.enable: true` |
| 全站配色 | `themes\shiguang\source\css\style.css` | 最上面 `:root` 里的 `--main` |
| 备案号、页脚链接 | `themes\shiguang\_config.yml` | 搜 `footer:` |

---

## 1. 发一篇文章

### 1.1 新建

在 `D:\网站` 里执行：

```
npm run new -- "文章标题" "分类名"
```

例如：

```
npm run new -- "雨天便利店" "短篇小说"
```

会自动生成两个东西：

- 文章文件：`source\_posts\短篇小说\2026-09-16-雨天便利店.md`
- 同名配图文件夹：`source\_posts\短篇小说\2026-09-16-雨天便利店\`

然后用记事本、Typora 或 VSCode 打开那个 `.md` 文件写就行。

> **分类名不用事先创建**，命令会自动建文件夹。
> 想加一个新分类，比如「读书笔记」，直接 `npm run new -- "书名" "读书笔记"` 即可。

### 1.2 头部信息怎么写

文件开头 `---` 到 `---` 之间的部分是「头部信息」：

```markdown
---
title: 文章的标题
date: 2026-09-16 21:30:00
categories: 短篇小说
cover: /img/cover-default.svg
excerpt: 一句话摘要，会显示在首页卡片上。
---

正文从这里开始写，空一行就是一个新段落。
```

| 字段 | 作用 | 必须吗 |
| --- | --- | --- |
| `title` | 文章标题 | 必须 |
| `date` | 发布日期（决定排序） | 必须 |
| `categories` | 分类，和文件夹名保持一致 | 必须 |
| `cover` | 列表和文章页的顶部大图 | 可以不写 |
| `excerpt` | 首页卡片上的一句话摘要 | 可以不写 |
| `featured: true` | 让这篇文章出现在首页横幅推荐位 | 可以不写 |

> 标签（tags）功能已经去掉了，现在只用分类。

### 1.3 配图

把图片放进**那个同名文件夹**里（比如 `2026-09-16-雨天便利店\`），然后在正文里写：

```markdown
![](照片.png)
```

### 1.4 写完别忘了发布

先 `Ctrl + C` 停掉预览，然后：

```
npm run deploy
```

---

## 2. 加 / 改友情链接 ⭐

友情链接显示在「关于我」页面（<https://annansup.com/about/>）的下半部分。

### 2.1 加一个朋友

打开 `D:\网站\source\_data\links.json`，在 `friends` 的方括号里加一段：

```json
{
  "friends": [
    {
      "name": "小王",
      "url": "https://xiaowang.com",
      "avatar": "/img/friends/xiaowang.png",
      "desc": "爱写影评的邻居"
    }
  ]
}
```

**加多个就在后面接着写，用逗号隔开**（最后一条后面**不要**加逗号）：

```json
{
  "friends": [
    {
      "name": "小王",
      "url": "https://xiaowang.com",
      "avatar": "/img/friends/xiaowang.png",
      "desc": "爱写影评的邻居"
    },
    {
      "name": "小李",
      "url": "https://xiaoli.cn",
      "avatar": "/img/friends/xiaoli.jpg",
      "desc": "写小说的高产选手"
    }
  ]
}
```

四个字段的意思：

| 字段 | 填什么 |
| --- | --- |
| `name` | 朋友的站名或昵称 |
| `url` | 他的网站地址，**要带 `https://`** |
| `avatar` | 头像图片，见下面 2.2 |
| `desc` | 一句话介绍 |

### 2.2 头像怎么准备

**步骤：**

1. 把朋友的头像图片（png / jpg / svg 都行，建议正方形、100×100 以上）复制到
   `D:\网站\source\img\friends\` 文件夹里
2. 文件名建议用英文，比如 `xiaowang.png`
3. 在 `links.json` 里把 `avatar` 写成 **`/img/friends/文件名`**

举几个例子：

| 你放进文件夹的文件 | `avatar` 就写 |
| --- | --- |
| `source\img\friends\xiaowang.png` | `"/img/friends/xiaowang.png"` |
| `source\img\friends\小李.jpg` | `"/img/friends/小李.jpg"` |
| 不填 `avatar` | 会显示默认头像 `/img/avatar.svg` |

> **不建议直接填别人网站的图片网址。** 很多网站有防盗链，图片在你站上会显示成破图。
> 下载到本地最稳妥。

### 2.3 改、删、调整顺序

- **改**：直接改那一段里的文字
- **删**：把那一整段 `{ ... }` 删掉。**注意别漏掉逗号**——删中间一条时，要保证剩下的条目之间都有逗号
- **调整顺序**：`links.json` 里的前后顺序，就是页面上显示的顺序，上下挪动整段 `{ ... }` 即可

> JSON 格式很严格：**引号必须是英文的 `"`，不能是中文的 `“`**。
> 如果刷新后友链整个不见了，八成是这里写错了（可以用 <https://jsonlint.com> 检查）。

### 2.4 「我的主页」那几张卡片（不是友链）

关于页上面那排**网易云 / 哔哩哔哩 / Steam / Bangumi / 抖音**卡片，**不在 `links.json` 里**，而在：

`themes\shiguang\_config.yml` → 搜 `author_card:` → 下面的 `social:`

```yaml
social:
  steam:
    name: Steam
    link: https://steamcommunity.com/id/annansup/    # ← 改这里
    icon: steam
    color: '#1b2838'
    desc: 游戏时长与成就
```

改 `link:` 就行，改动会同时作用到关于页、侧边栏和页脚。

### 2.5 发布

改完 `Ctrl + C` 停掉预览，然后：

```
npm run deploy -- "更新友链"
```

约 1 分钟后打开 <https://annansup.com/about/> 检查。

---

## 3. 改我自己的信息

打开 `themes\shiguang\_config.yml`（用记事本或 VSCode），里面每一段上面都有中文注释：

- **昵称**：`author_card:` 下面的 `name:`
- **头像**：`avatar:` 那一行
- **网易云 / B站 / Steam / Bangumi / 抖音**：`social:` 下面，把 `link:` 换成你自己的主页地址

站名（网页标题栏显示的名字）在**另一个文件**：根目录的 `_config.yml`，改 `title:`。

---

## 4. 换音乐

1. 打开文件夹 `D:\网站\source\music\`
2. 把你的 mp3 直接复制进去（支持 mp3 / m4a / ogg / wav / flac，可以建子文件夹）
3. 重启预览 `npm run server`，然后点左下角圆形按钮听

文件名会直接当成歌名显示，建议写成 `歌手 - 歌名.mp3`。

**播放器说明**

- 位置在**顶部导航栏**，只有一个音符按钮：点一下播放、再点一下暂停
- 鼠标移到按钮上，才会展开「下一首」小按钮
- **播放顺序：按歌单顺序循环**。第一首放完自动接第二首，最后一首放完回到第一首
- 歌单顺序 = 文件名按拼音/字母排序（所以名字统一写成 `歌手 - 歌名.mp3` 会比较整齐）
- 站内切页音乐不断（用的是局部刷新）
- 默认音量 **0.7**，在 `themes\shiguang\_config.yml` 的 `music.volume` 改（范围 0~1）

想改回**随机播放**，把同一个文件里的 `music.shuffle` 改成 `true`。

**点了没声音？按这个顺序查：**

1. **是不是刚点下去就以为没反应？** 歌单加载需要一点时间，第一次点会显示「歌单加载中…」，加载完会自动开始播。留意导航栏里有没有出现歌名
2. **看歌名出来了吗？** 歌名出现了说明在播，那就是**音量问题**：
   - 先加大系统音量 / 检查有没有连蓝牙耳机
   - 再把 `music.volume` 从 0.7 往上调到 0.9 或 1
3. **浏览器拦住了自动播放**：会提示「浏览器拦住了播放，再点一次按钮试试」，**再点一次**即可
4. **提示「歌单加载失败」**：刷新页面重试
5. **提示「这首放不出来」**：会自动跳到下一首；如果连着好几首都这样，检查 `source\music\` 里的文件是不是损坏，或者文件名里有特殊字符

---

## 5. 换图片

都在 `themes\shiguang\source\img\`：

| 文件 | 用在哪 |
| --- | --- |
| `logo.png` | 左上角站点标志 |
| `favicon.png` | 浏览器标签页的小图标 |
| `avatar.svg` | 侧边栏头像 |
| `cover-default.svg` | 文章没有单独封面时的默认图 |
| `cover-writing.svg`、`cover-game.svg` … | 示例文章用的封面 |

换法就是**用同名文件覆盖**。

---

## 6. 改分类（"文章"菜单里的归档）

分类跟文件夹走，**不用改任何设置**：

1. 在 `source\_posts\` 里新建一个文件夹，比如 `读书笔记`
2. 把文章放进这个文件夹，并在文章头部写 `categories: 读书笔记`
3. 鼠标放到顶部「文章」上，就会多出「读书笔记」这一项，右边还带篇数

用 `npm run new -- "标题" "分类名"` 建文章时，文件夹会自动创建。

---

## 7. 改首页横幅和公告

打开 `themes\shiguang\_config.yml`：

- `banner:` → `title`（大标题）、`intro`（副标题）、`url_text`（那行小英文）
- `banner.tabs` → 横幅上那四个按钮：`label` 是文字，`icon` 是图标，`href` 是点进去的页面，`color` 是颜色
- `notice:` → 顶部那条公告，`text` 改文字，`enable: false` 关闭
- `menu:` → 顶部导航。写法是 `名字: 链接`（点进去）或者 `名字:` 下面缩进写子菜单

---

## 8. 改配色

打开 `themes\shiguang\source\css\style.css`，最上面有：

```css
--main: #216e99;    /* 主色，按钮和链接 */
--accent: #c6452b;  /* 强调色，比如播放中的按钮 */
--bg: #eef2f7;      /* 页面底色 */
```

把色号换掉就行（现在用的是靛青、朱砂这些中国传统色）。深色模式在下面 `html[data-theme='dark']` 那一段。

---

## 9. 游戏 / 电影 / 动漫三个页面

里面**已经是你自己的真实数据**了。想更新有两种方式：

- **手动**：直接编辑 `source\_data\games.json`、`movies.json`、`anime.json`
- **自动抓取**：把根目录 `.env.example` 复制成 `.env`，填上 Steam / Bangumi / 豆瓣账号，然后执行

```
npm run fetch
```

### 9.1 隐藏不想看到的游戏

打开 `source\_data\games-hidden.json`，把 appid 或游戏全名填进去：

```json
{
  "ids": ["2622000", "1714320"],
  "titles": ["Tom Clancy's Rainbow Six Siege - Test Server"]
}
```

改完执行 `npm run fetch:steam` 就会从页面上消失。

### 9.2 写我的短评

短评**不会从豆瓣 / Bangumi 抓取**，全部自己写。打开对应的 JSON，找到那一条，填 `"comment"`：

```json
{
  "title": "千与千寻",
  "rating": 9.4,
  "comment": "每隔几年重看一次，每次都能看到新的东西。"
}
```

**短评在电脑和手机上的显示方式不一样**（这是刻意的）：

| 设备 | 短评怎么显示 |
| --- | --- |
| 电脑 | 平时不显示；**鼠标停在卡片上**约 0.3 秒，右侧浮出一张小卡片显示短评 |
| 手机 / 平板 | **直接显示在卡片里**、标题下方，灰色小字，自动换行（最多 6 行，超过会截断加省略号） |

手机没有「悬停」这个动作，所以改成平铺显示——不用点、不用悬停，一眼就能看到。

> ⚠️ 改完 `_data` 里的 JSON 后，本地预览有时**不会自动刷新**（Hexo 的监视器不监听 `_data` 目录）。
> 按 `Ctrl + C` 停掉再 `npm run server` 重启即可。发布到线上不受影响，因为线上是完整重新生成的。

### 9.3 换某一张封面

游戏封面在 `source\img\covers\steam\`，文件名就是 appid（例如 `323190.webp` 是冰汽时代），按同名覆盖即可，推荐 600×900 竖版图。

豆瓣和 Bangumi 的图床有防盗链，所以封面是**下载到自己站里**的，位置在 `source\img\covers\`。想换就把新图放进去，再在对应 JSON 里把 `cover` 改成本地路径，例如 `"cover": "/img/covers/1291561.jpg"`。

---

## 10. 常用命令

| 命令 | 作用 |
| --- | --- |
| `npm run server` | 启动本地预览（最常用） |
| **`npm run deploy`** | **发布上线（改完必须执行）** |
| `npm run deploy -- "说明"` | 发布，并写一句提交说明 |
| `npm run new -- "标题" "分类"` | 新建一篇文章 |
| `npm run fetch` | 抓取游戏 / 电影 / 动漫数据 |
| `npm run build` | 只在本机生成正式网页文件（在 `public` 文件夹） |
| `npm run clean` | 清理缓存（页面显示错乱时用） |

> 这些命令都要在 `D:\网站` 文件夹里执行。

### 10.1 网站是怎么上线的（看不懂可以跳过）

- 你的所有内容都存在 GitHub 仓库 `AnnanSuper/annansup.github.io-blog`
- `npm run deploy` 把改动推上去后，**GitHub 会自动重新生成整个网站**并发布
- 域名 `annansup.com` 解析到 GitHub Pages，HTTPS 证书由 GitHub 自动申请和续期

所以：**你不需要手动上传任何文件，也不需要管服务器。**

### 10.2 想知道站点是否在线

浏览器打开 <https://annansup.com>，能看到就是正常的。

---

## 11. 出错怎么办

### ⭐ 最常见：`npm error code ENOENT` / `Could not read package.json`

完整报错长这样：

```
npm error code ENOENT
npm error path C:\WINDOWS\system32\package.json
npm error enoent Could not read package.json: no such file or directory
```

**原因：命令执行时所在的文件夹不对。** `npm` 只在**当前所在文件夹**里找 `package.json`，它不会自己去 D 盘找。

看报错里的 `path` 后面那一串——如果**不是** `D:\网站\package.json`，就是这个问题。

**怎么修（任选一种）：**

1. **最省事**：别敲命令了，直接双击 `D:\网站\启动预览.bat` 或 `发布上线.bat`
2. **先切目录再敲命令**：在 PowerShell 里执行

   ```
   cd D:\网站
   npm run server
   ```

   想确认自己在哪个目录，执行 `pwd`（或看命令提示符前面显示的路径）。

**为什么会跑到 `system32`？** 从开始菜单、Win+X 菜单、或「以管理员身份运行」打开的 PowerShell，默认就停在 `C:\WINDOWS\system32`。只有**在文件夹里 Shift+右键**打开，才会停在那个文件夹。

> 这个报错**和网站、和你的代码完全无关**，纯粹是站错了地方。

### 发布相关

**Q：执行了 `npm run deploy`，但网站上还是旧的？**

1. 部署需要时间，**先等 1～2 分钟**
2. 然后按 `Ctrl + F5` 强制刷新（普通刷新可能读到缓存）
3. 还不行就去 <https://github.com/AnnanSuper/annansup.github.io-blog/actions> 看部署记录：
   - 绿色 ✓ = 部署成功，问题在浏览器缓存
   - 红色 ✗ = 部署失败，把红色的报错内容发我

**Q：提示 `找不到 git 命令` / `git is not recognized`？**

关掉 PowerShell 窗口重新开一个再试。如果还不行，说明 PATH 没生效，重新登录一次 Windows 即可。

**Q：`npm run deploy` 提示推送失败、连接超时？**

多半是网络。**先确认 Clash 代理开着**（发布脚本会自动探测本机 `127.0.0.1:7890`）。Clash 没开就会连不上 GitHub。

**Q：提示 `变基冲突`？**

说明 GitHub 上有我这边改过、你本地没有的内容。执行 `git rebase --abort` 放弃这次操作，然后把完整报错发我。

### 本地预览相关

| 现象 | 怎么办 |
| --- | --- |
| 页面打不开 | 确认那个黑窗口还开着；端口被占用就执行 `npx hexo server -p 4001` |
| 改了没反应 | 先 `Ctrl + C` 停掉，再 `npm run server` 重启 |
| 画面错乱 | 执行 `npm run clean`，再 `npm run server` |
| 想彻底重来 | 删掉 `D:\网站\public` 文件夹和 `db.json`，重新 `npm run server` |

### 友链相关

| 现象 | 原因 |
| --- | --- |
| 友链整个不见了 | `links.json` 格式写错了（中文引号、多/少逗号）→ 用 <https://jsonlint.com> 检查 |
| 头像显示成破图 | 图片没放进 `source\img\friends\`，或 `avatar` 路径写错了（要以 `/img/friends/` 开头） |
| 只有某一条没显示 | 那一条少了大括号或逗号 |

---

## 12. 最后

改任何东西的完整节奏就是：

```
1. 改文件
2. Ctrl + C          ← 停掉预览
3. npm run deploy    ← 发布
4. 等 1 分钟，刷新 https://annansup.com
```

第 2、3 步可以合并成：**双击 `发布上线.bat`**。

有任何一步卡住，把**完整的报错文字**发我，我帮你看。
