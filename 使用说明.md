# 梦鸼博客 · 使用说明

不用懂代码，照着做就行。**改完任何东西后，如果页面没变化，就把预览重启一次**（关掉黑窗口，重新执行下面的启动命令）。

## 0. 先会这两件事

启动网站（在 `D:\网站` 文件夹里按住 Shift + 右键 → 打开 PowerShell，然后输入）：

```
npm run server
```

浏览器打开 <http://localhost:4000> 就能看。看完按 `Ctrl + C` 停止。

---

## 一张表：想改什么 → 打开哪个文件

| 我想改… | 打开这个文件 | 改哪里 |
| --- | --- | --- |
| 发文章 | 新建 `source\_posts\分类名\标题.md` | 直接写正文（见第 2 节） |
| 我的昵称、站名 | `_config.yml` | `title:` 和 `author:` |
| 头像 | 替换 `themes\shiguang\source\img\avatar.svg` | 换个同名图片文件即可 |
| Logo（左上角图标） | 替换 `themes\shiguang\source\img\logo.png` | 建议正方形、透明底 |
| 网易云 / B站 / Steam / Bangumi 链接 | `themes\shiguang\_config.yml` | 搜 `social:`，改 `link:` 那一行 |
| 右下角音乐 | 把 mp3 丢进 `source\music\` | 文件名就是歌名 |
| 文章分类（"文章"菜单里的归档） | 在 `source\_posts\` 里新建文件夹 | 文件夹名＝分类名 |
| 首页大标题、公告、四个按钮 | `themes\shiguang\_config.yml` | 搜 `banner:` 和 `notice:` |
| 友情链接 | `source\_data\links.json` | 往 `friends` 里加一条 |
| 游戏 / 电影 / 动漫列表 | `source\_data\games.json` 等 | 也可以自动抓取（第 10 节） |
| 不想显示某个游戏 | `source\_data\games-hidden.json` | 填 appid 或游戏全名，再跑 `npm run fetch:steam` |
| 某张卡片想换封面 | 覆盖 `source\img\covers\steam\` 里的 `appid.webp`（豆瓣/Bangumi 同理） | |
| 写我的短评 | `source\_data\movies.json` 等，找那一行的 `"comment"` | |
| 播放器音量 | `themes\shiguang\_config.yml` | `music.volume: 0.35` |
| 页面切换不中断音乐 | `themes\shiguang\_config.yml` | `pjax.enable: true` |
| 某个收藏条目的封面图 | 把图片放进 `source\img\covers\`，然后在 JSON 里把 `cover` 改成 `/img/covers/图片名.jpg` | |
| 全站配色 | `themes\shiguang\source\css\style.css` | 最上面 `:root` 里的 `--main` |
| 备案号、页脚链接 | `themes\shiguang\_config.yml` | 搜 `footer:` |

---

## 1. 发一篇文章

**方法一（推荐，最省事）**

在 `D:\网站` 里执行：

```
npm run new -- "文章标题" "分类名"
```

例如 `npm run new -- "雨天便利店" "短篇小说"`，会自动生成文件并在编辑器里填好开头的设置。

**方法二（手动）**

1. 打开文件夹 `D:\网站\source\_posts\`
2. 在里面新建一个文件夹，**文件夹名就是分类名**，比如 `随笔`、`短篇小说`、`游戏图文攻略`
3. 在这个文件夹里新建一个文本文件，把名字改成 `我的标题.md`（后缀必须是 `.md`）
4. 用记事本或 Typora 打开，照下面这个样子写：

```markdown
---
title: 文章的标题
date: 2026-09-14 21:30:00
categories: 随笔
cover: /img/cover-default.svg
excerpt: 一句话摘要，会显示在首页卡片上
---

正文从这里开始写，空一行就是一个新段落。

## 这是小标题

- 这是列表
- 第二条
```

上面两条 `---` 之间的部分叫「头部信息」，不用全写，最少写 `title` 和 `date` 就行。

写法小抄：

| 想要的效果 | 怎么写 |
| --- | --- |
| 小标题 | `## 标题` |
| 加粗 | `**加粗**` |
| 链接 | `[文字](网址)` |
| 插入图片 | 把图片放进文章同名的文件夹，然后写 `![](图片名.png)` |
| 引用 | 行首加 `> ` |
| 代码 | 用三个反引号包起来 |

---

## 2. 改我自己的信息

打开 `themes\shiguang\_config.yml`（用记事本或 VSCode），里面每一段上面都有中文注释：

- **昵称**：`author_card:` 下面的 `name:`
- **头像**：`avatar:` 那一行
- **网易云 / B站 / Steam / Bangumi**：`social:` 下面，把 `link:` 换成你自己的主页地址
- **右上角社交图标**：和上面是同一处，改完侧边栏、页脚、关于页都会跟着变

站名（网页标题栏显示的名字）在**另一个文件**：根目录的 `_config.yml`，改 `title:`。

---

## 3. 换音乐

1. 打开文件夹 `D:\网站\source\music\`
2. 把你的 mp3 直接复制进去（支持 mp3 / m4a / ogg / wav / flac，可以建子文件夹）
3. 重启预览：`npm run server`
4. 网页左下角有个圆形按钮，点一下就会随机播放

文件名会直接当成歌名显示，建议写成 `歌手 - 歌名.mp3`。


音量在 `themes\shiguang\_config.yml` 的 `music:` → `volume:`。

播放器在**顶部导航栏**里：只有一个音符按钮，**点一下播放、再点一下暂停**；
鼠标移上去才会展开「下一首」的小按钮（不用的时候收起来，不挡视线）。默认音量 0.35，可在主题配置里改。
点一下随机播一首，**一首放完会自动接着播下一首（循环播放）**，也可以点「停止」。
**歌名会在导航栏里滚动显示**，名字再长也不会被截掉。
站内切换页面（点导航、点文章）时**音乐不会中断**——站点用的是局部刷新，播放器一直挂在页面上。

（循环可以在 `themes\shiguang\_config.yml` 里用 `music.loop: false` 关掉；`pjax.enable: false` 可以关掉局部刷新。）

---

## 4. 换图片

都在这个文件夹：`themes\shiguang\source\img\`

| 文件 | 用在哪 |
| --- | --- |
| `logo.png` | 左上角站点标志（现在是 SOS 团） |
| `favicon.png` | 浏览器标签页的小图标 |
| `avatar.svg` | 侧边栏头像 |
| `cover-default.svg` | 文章没有单独封面时的默认图 |
| `cover-writing.svg`、`cover-game.svg` … | 示例文章用的封面，可换成自己的图 |

换法就是**用同名文件覆盖**（比如把新头像命名成 `avatar.svg` 放进去）。

---

## 5. 改分类（"文章"菜单里的归档）

分类跟文件夹走，**不用改任何设置**：

1. 在 `D:\网站\source\_posts\` 里新建一个文件夹，比如 `读书笔记`
2. 把文章放进这个文件夹，并在文章头部写 `categories: 读书笔记`
3. 重启预览 —— 鼠标放到顶部「文章」上，就会多出「读书笔记」这一项，右边还带篇数

想删掉某个分类，就把那个文件夹里的文章移走或删掉，文件夹本身删掉即可。

> 顺便说：标签功能已经按你的要求去掉了，现在只用分类。

---

## 6. 改首页横幅和公告

打开 `themes\shiguang\_config.yml`：

- `banner:` → `title`（大标题）、`intro`（副标题）、`url_text`（那行小英文）
- `banner.tabs` → 横幅上那四个按钮：`label` 是文字，`icon` 是图标，`href` 是点进去的页面，`color` 是颜色
- `notice:` → 顶部那条公告，`text` 改文字，`enable: false` 关闭
- `menu:` → 顶部导航。写法是 `名字: 链接`（点进去）或者 `名字:` 下面缩进写子菜单

---

## 7. 改配色

打开 `themes\shiguang\source\css\style.css`，最上面有：

```css
--main: #216e99;    /* 主色，按钮和链接 */
--accent: #c6452b;  /* 强调色，比如播放中的按钮 */
--bg: #eef2f7;      /* 页面底色 */
```

把里面的色号换掉就行（现在用的是靛青、朱砂这些中国传统色）。深色模式在下面 `html[data-theme='dark']` 那一段。

---

## 8. 友链

打开 `source\_data\links.json`，照着格式加：

```json
{
  "friends": [
    { "name": "朋友的站名", "url": "https://他的网址", "avatar": "头像图片网址", "desc": "一句话介绍" }
  ]
}
```

加完刷新「关于我」页面就能看到。

---

## 9. 游戏 / 电影 / 动漫三个页面

现在里面**已经是你自己的真实数据**了（游戏 114 款 / 电影 156 部 / 动漫 185 部）。想更新的话有两种方式：

- **手动**：直接编辑 `source\_data\games.json`、`movies.json`、`anime.json`
- **自动抓取**：把根目录 `.env.example` 复制成 `.env`，填上你的 Steam / Bangumi / 豆瓣账号，然后执行

```
npm run fetch
```

详细说明见根目录的 `README.md`。

### 9.1 卡片长什么样

- **平时**：只显示封面和名字
- **鼠标放上去停 0.3 秒**：卡片像被"拉近"一样放大，网页其它部分变模糊，
  同时右侧弹出一张小卡片——右上角是**小字的名字**，下面一条**横线**，横线下面是**我写的短评**
  （字号 16px，文字多了会自动缩小）。**如果还没写短评，就只显示那条横线**
- 鼠标移开，一切恢复原样
- 卡片左上角有个小角标（在玩 / 已玩过 / 未开始），是用来配合上面筛选按钮的
- 卡片高度：游戏 2:3（和你的封面比例一致），电影和动漫 0.72，稍微矮一点、不裁图

### 9.2 隐藏不想看到的游戏

打开 `source\_data\games-hidden.json`，把 appid 或游戏全名填进去：

```json
{
  "ids": ["2622000", "1714320", "4704690"],
  "titles": ["Tom Clancy's Rainbow Six Siege - Test Server"]
}
```

改完执行 `npm run fetch:steam` 就会从页面上消失（当前已隐藏 12 款）。
家庭共享库的游戏现在按普通游戏展示，不再单独标注。

### 9.3 写我的短评

短评**不会从豆瓣 / Bangumi 抓取**（以后也不会），全部由你自己写。打开对应的 JSON，
找到那一条，把 `"comment"` 后面填上文字即可：

```json
{
  "title": "千与千寻",
  "rating": 9.4,
  "comment": "每隔几年重看一次，每次都能看到新的东西。"
}
```

`rating` 是"我的评分"（0–10），会显示在悬停面板里。

### 9.4 换某一张封面

游戏封面在 `source\img\covers\steam\`，文件名就是 appid（例如 `323190.webp` 是冰汽时代），
把新图按同名覆盖即可，推荐 600×900 的竖版图。

> 如果从 Steam 客户端导出的 grid 素材里某张是动画首帧（比如冰汽时代那种一片白），
> 可以直接用官方竖版图：`https://cdn.cloudflare.steamstatic.com/steam/apps/<appid>/library_600x900.jpg`

**不想显示的游戏**：把 appid 或游戏全名填进 `source\_data\games-hidden.json` 的 `ids` / `titles`，
然后执行 `npm run fetch:steam` 就会从页面上去掉。例如：

```json
{
  "ids": ["431960"],
  "titles": ["Tom Clancy's Rainbow Six Siege - Test Server"]
}
```

**卡片上的信息卡**：鼠标放到任意收藏卡片上，卡片会放大，并浮出标题、原始标题、时长/集数、
我的评分和平台评分，最下面是"我的短评"（现在都还没写，会显示"还没有写短评"）。
短评写在对应的 JSON 里即可，例如 `source\_data\movies.json` 里给某一条的 `"comment"` 填上文字。
另外抓取脚本默认**不会**把你在豆瓣/Bangumi 写过的短评抓过来（想抓的话把 `_config.yml` 里
`fetch.douban.import_comments` 设成 `true`）。

**关于封面图**：豆瓣和 Bangumi 的图床有防盗链，网页直接引用它们的图片地址会被拒绝显示，
所以封面是**下载到自己站里**的，位置在 `source\img\covers\`。想换某一张封面，
把新图片放进这个文件夹，然后在 `source\_data\movies.json` 里把那一行的 `cover` 改成本地路径即可，
例如 `"cover": "/img/covers/1291561.jpg"`。

---

## 10. 常用命令

| 命令 | 作用 |
| --- | --- |
| `npm run server` | 启动本地预览（最常用） |
| `npm run build` | 生成正式网页文件（在 `public` 文件夹） |
| `npm run new -- "标题" "分类"` | 新建一篇文章 |
| `npm run fetch` | 抓取游戏 / 电影 / 动漫数据 |
| `npm run clean` | 清理缓存（页面显示错乱时用） |

---

## 11. 出错怎么办

- **页面打不开**：确认那个黑窗口还开着；端口被占用就执行 `npx hexo server -p 4001`
- **改了没反应**：先按 `Ctrl + C` 停掉，再 `npm run server` 重启
- **画面错乱**：执行 `npm run clean` 再 `npm run server`
- **想换回默认样子**：删掉 `D:\网站\public` 文件夹和 `db.json`，重新 `npm run server`
